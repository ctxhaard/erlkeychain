# Erlang Keychain manager based on ncurses

## Build
```shell
$ rebar3 compile
```

## Launch
```shell
$ rebar3 shell
```

## Release
```shell
$ rebar3 release
# to test the release
_build/default/rel/keychain/bin/keychain console
```